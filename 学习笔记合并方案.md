# 学习笔记自动合并方案

## 1. 现状分析

### 1.1 当前行为

```
generateStudyNote()
    │
    ├─ 文件名：{docName}学习笔记.md （如：研报分析学习笔记.md）
    │
    ├─ 内容：H1 标题 + 文档名 + 页码 + 时间 + 选中的 Q&A
    │
    └─ saveMarkdownNote(path, fileName, content)
           │
           └─ QFile::Truncate → ❌ 每次都覆盖！旧的笔记丢失
```

### 1.2 问题

| # | 问题 |
|---|------|
| **P1** | 每次生成笔记都**覆盖**之前的内容，同一个文档的历史笔记全部丢失 |
| **P2** | 无法追溯某段笔记对应文档的哪一页 |
| **P3** | 多次阅读同一文档产生的笔记分散在多个文件中（如果用户手动改名避免覆盖） |

---

## 2. 方案设计

### 2.1 核心思路

**同一文档 → 同一笔记文件 → 追加写入 → 页码标注**

```
第一次生成（文件不存在）          第二次生成（文件已存在）          ...
─────────────────────────       ─────────────────────────       ───
# 研报分析 学习笔记              # 研报分析 学习笔记              （同上）
                                ...
> 创建时间：2026-07-04                                   ← 文件头部（元信息）
                                ---                          
---                             ## 📍 第 12 页 — 07-04     ← 追加条目 2
## 📍 第 5 页 — 07-04 10:30     ### ❓ 问题
### ❓ 问题                      ...
...                             ### 💡 回答
### 💡 回答                      ...
...                             ---
                                ## 📍 第 3 页 — 07-05      ← 追加条目 3
                                ...
```

### 2.2 判定逻辑

```
generateStudyNote()
    │
    ├─ 计算笔记文件路径 = dir(htmlPath) / {docName}学习笔记.md
    │
    ├─ QFileInfo::exists(path) ?
    │
    ├─ NO  → 新建模式：写入完整头部 + 第一条笔记
    │
    └─ YES → 追加模式：读取现有文件 → 追加新条目（含页码标记）
```

---

## 3. 详细设计

### 3.1 内容结构规范

#### 新建模式（文件不存在）

```markdown
# {文档名} 学习笔记

> 📄 文档：{文档名}
> 📅 创建时间：2026-07-04 10:30:00
> 📂 来源：{htmlPath}

---

## 📍 第 5 页 — 2026-07-04 10:30:00

### ❓ 问题

这里是对应的用户问题内容...

### 💡 回答

这里是对应的 Agent 回答内容...

```

#### 追加模式（文件已存在）

在文件末尾追加以下内容：

```markdown
---

## 📍 第 12 页 — 2026-07-04 14:20:00

### ❓ 问题

用户问题...

### 💡 回答

Agent 回答...

```

**规则**：
- 每个笔记条目以 `---`（水平线）分隔
- 每条笔记以 `## 📍 第 N 页 — 时间戳` 作为二级标题（注明年份-月-日 时:分:秒）
- 问题和回答使用三级标题 `###` 区分
- 同一页多次生成 → 每次都是独立条目，通过时间戳区分

---

### 3.2 C++ 层改动：`PdfConverter`

#### 3.2.1 修改 `saveMarkdownNote` — 支持追加

**文件**：`src/services/PdfConverter.h`

```cpp
// 旧签名
Q_INVOKABLE bool saveMarkdownNote(const QString &htmlPath,
                                   const QString &fileName,
                                   const QString &content) const;

// 新签名：新增 append 参数，默认 false（向后兼容）
Q_INVOKABLE bool saveMarkdownNote(const QString &htmlPath,
                                   const QString &fileName,
                                   const QString &content,
                                   bool append = false) const;
```

**文件**：`src/services/PdfConverter.cpp`

```cpp
bool PdfConverter::saveMarkdownNote(const QString &htmlPath,
                                     const QString &fileName,
                                     const QString &content,
                                     bool append) const
{
    if (htmlPath.isEmpty() || fileName.isEmpty()) return false;

    const QFileInfo fi(htmlPath);
    const QString dir = fi.absolutePath();
    if (dir.isEmpty()) return false;

    QString name = fileName;
    if (!name.endsWith(".md", Qt::CaseInsensitive))
        name += ".md";

    const QString path = QDir(dir).absoluteFilePath(name);

    // ★ 追加模式：检查文件是否以换行结尾，保证分隔符正确
    QIODevice::OpenMode mode = QIODevice::WriteOnly | QIODevice::Text;
    if (append && QFileInfo::exists(path)) {
        mode |= QIODevice::Append;

        // 确保追加内容前有一个空行
        QFile check(path);
        if (check.open(QIODevice::ReadOnly)) {
            qint64 size = check.size();
            if (size > 0) {
                check.seek(size - 1);
                char lastChar = 0;
                check.getChar(&lastChar);
                if (lastChar != '\n') {
                    check.close();
                    // 先写一个换行再追加
                    QFile prepend(path);
                    if (prepend.open(QIODevice::Append | QIODevice::Text)) {
                        prepend.write("\n");
                        prepend.close();
                    }
                }
            }
            check.close();
        }
    } else {
        mode |= QIODevice::Truncate;
    }

    QFile f(path);
    if (!f.open(mode))
        return false;

    QTextStream out(&f);
    out.setEncoding(QStringConverter::Utf8);
    out << content;
    f.close();

    qDebug() << "[PdfConverter] saved markdown note (append=" << append
             << "):" << path;
    return true;
}
```

> **注意**：`QIODevice::Append` 自动 seek 到末尾，不需要手动 seek。上述换行检查是可选的保护措施。

---

### 3.3 QML 层改动：`KnowledgeBasePage.qml`

#### 3.3.1 修改 `generateStudyNote()`

```qml
function generateStudyNote() {
    if (selectedHtmlPath === "") {
        errorBar.text = "请先打开一个 HTML 文档"
        errorBar.visible = true
        errorTimer.restart()
        return
    }

    let selectedCount = 0
    for (let i = 0; i < kbChatModel.count; i++) {
        if (kbChatModel.get(i).selectedForNote) selectedCount++
    }
    if (selectedCount === 0) {
        errorBar.text = "请勾选要加入笔记的对话"
        errorBar.visible = true
        errorTimer.restart()
        return
    }

    const fileName = selectedHtmlPath.split(/[/\\]/).pop()
    const docName = fileName.replace(/\.html?$/i, "")
    const noteFileName = docName + "学习笔记"
    const timestamp = Qt.formatDateTime(new Date(), "yyyy-MM-dd hh:mm:ss")

    // ★ 检查笔记文件是否已存在
    const noteDir = selectedHtmlPath.substring(0, selectedHtmlPath.lastIndexOf('/'))
    const notePath = noteDir + "/" + noteFileName + ".md"
    const fileExists = pdfConverter.fileExists(notePath)
    
    let content = ""

    if (fileExists) {
        // ═══ 追加模式 ═══
        content += "\n---\n\n"
        content += "## 📍 第 " + htmlCurrentPage + " 页 — " + timestamp + "\n\n"
    } else {
        // ═══ 新建模式 ═══
        content += "# " + docName + " 学习笔记\n\n"
        content += "> 📄 文档：" + docName + "\n"
        content += "> 📅 创建时间：" + timestamp + "\n"
        content += "> 📂 来源：" + selectedHtmlPath + "\n\n"
        content += "---\n\n"
        content += "## 📍 第 " + htmlCurrentPage + " 页 — " + timestamp + "\n\n"
    }

    // 拼接选中的 Q&A
    for (let i = 0; i < kbChatModel.count; i++) {
        const m = kbChatModel.get(i)
        if (!m.selectedForNote) continue
        if (m.role === "user") {
            content += "### ❓ 问题\n\n" + m.content + "\n\n"
        } else if (m.role === "assistant") {
            content += "### 💡 回答\n\n" + m.content + "\n\n"
        }
    }

    // ★ 调用 saveMarkdownNote，append 参数由文件是否存在决定
    if (pdfConverter.saveMarkdownNote(selectedHtmlPath, noteFileName, content, fileExists)) {
        loadHtmlFiles()
        errorBar.color = "#E8F5E9"
        errorBar.border.color = "#81C784"
        errorBar.text = fileExists 
            ? "笔记已追加到：" + noteFileName + ".md（第 " + htmlCurrentPage + " 页）"
            : "笔记已创建：" + noteFileName + ".md"
        errorBar.visible = true
        errorTimer.restart()
    } else {
        errorBar.color = "#FFEBEE"
        errorBar.border.color = "#F44336"
        errorBar.text = "保存笔记失败"
        errorBar.visible = true
        errorTimer.restart()
    }
}
```

#### 3.3.2 需要新增的辅助方法：`fileExists`

```cpp
// PdfConverter.h 新增
Q_INVOKABLE bool fileExists(const QString &path) const;

// PdfConverter.cpp 新增
bool PdfConverter::fileExists(const QString &path) const
{
    return QFileInfo::exists(path);
}
```

---

## 4. 文件改动清单

| 操作 | 文件 | 说明 |
|------|------|------|
| **修改** | `src/services/PdfConverter.h` | `saveMarkdownNote` 新增 `append` 参数（带默认值 false）；新增 `fileExists` 方法 |
| **修改** | `src/services/PdfConverter.cpp` | `saveMarkdownNote` 实现追加逻辑；新增 `fileExists` 实现 |
| **修改** | `qml/pages/KnowledgeBasePage.qml` | `generateStudyNote` 重写：文件存在检测 → 新建/追加分支 → 页码标注 |

---

## 5. 效果示例

### 文件 `研报分析学习笔记.md` 经过 3 次生成后的完整内容：

```markdown
# 研报分析 学习笔记

> 📄 文档：研报分析
> 📅 创建时间：2026-07-04 10:30:00
> 📂 来源：D:/knowledge_base/doc_1/index.html

---

## 📍 第 5 页 — 2026-07-04 10:30:00

### ❓ 问题

这家公司的营收增长率如何？

### 💡 回答

根据第5页的财报数据，该公司2025年营收增长率为23.5%...

---

## 📍 第 12 页 — 2026-07-04 14:20:00

### ❓ 问题

行业竞争格局分析

### 💡 回答

从行业分析章节来看，该公司在细分市场中排名第三...

---

## 📍 第 3 页 — 2026-07-05 09:15:00

### ❓ 问题

管理层背景如何？

### 💡 回答

董事长拥有20年行业经验...
```

### 左侧文件目录中显示：

```
📝 研报分析学习笔记.md      ← 只有一个文件，所有笔记合并在此
🌐 index.html
```

---

## 6. 兼容性说明

| 场景 | 行为 |
|------|------|
| `saveMarkdownNote` 不传 `append`（现有调用） | `append` 默认 `false`，行为与旧版完全一致 |
| 笔记文件被用户手动删除后再次生成 | `fileExists` 返回 false → 走新建模式 |
| 用户通过 `openNoteEditor` 手动编辑后再生成 | 追加模式正常工作（文件仍存在） |
| 不同文档的学习笔记 | 各自独立文件，文件名与文档名匹配，不会混淆 |

---

## 7. 可选增强（Phase 2）

| 增强项 | 说明 |
|--------|------|
| **添加目录（TOC）** | 每次追加后自动更新文件头部的目录索引，方便快速跳转到某一页的笔记 |
| **笔记预览** | 在文件列表中点击 .md 文件时，右侧 WebEngineView 渲染 Markdown 预览 |
| **同一页去重提示** | 同一页再次生成笔记时，弹出提示"第 N 页已有笔记，是否追加？" |
| **笔记导出** | 支持将所有笔记导出为单个 PDF |
